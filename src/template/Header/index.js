import * as React from "react";
import { HeaderComponent, Container } from "./styles";

const Header = ({ data }) => {
  return (
    <HeaderComponent>
      <Container></Container>
    </HeaderComponent>
  );
};

export default Header;
